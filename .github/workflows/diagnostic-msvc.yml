name: diagnostic-msvc

on:
  push:
    branches:
      - diag/wine-headless-getwindowtext-b133
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build:
    uses: VOTV-MP/Multivoid/.github/workflows/build-core.yml@v0.9.0n-b133-dev
    with:
      ref: ${{ github.sha }}
      source-sha: ${{ github.sha }}
      use-cache: true
